<view class="__sticky-item">
  <view class="{{isFixed?'__sticky-item-fixed':''}}">
    <view wx:if="title" class="__sticky-item-title">{{title}}</view>
    <block wx:else>
      <slot name="title"></slot>
    </block>
  </view>
  <slot></slot>
</view>