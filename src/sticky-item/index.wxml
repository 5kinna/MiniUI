<view class="__sticky-item {{isFixed?'__sticky-item-fixed':''}}">
  <view class="__sticky-item-top">
    <view wx:if="title" class="__sticky-item-title">{{title}}</view>
    <block wx:else>
      <slot name="title"></slot>
    </block>
  </view>
  <slot></slot>
</view>