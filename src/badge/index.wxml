<view class="badge">
  <slot></slot>
  <block wx:if="{{!disable}}">
    <view wx:if="{{dot}}" class="badge-dot mini-clz"></view>
    <view wx:else class="badge-text mini-clz">{{count>over?over+'+':count}}</view>
  </block>
</view>