<view class="__tag-{{size}}" hover-class="" style="{{ tagStyle }}" bindtap="onClick">
    <view class="__tag-txt"><slot></slot></view>
    <d-icon type="delete" size="32" catchtap="onClose" wx:if="{{ closable }}"></d-icon>
</view>